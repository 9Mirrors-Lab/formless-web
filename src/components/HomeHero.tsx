import { LayoutTestHeroSection } from './LayoutTestHeroSection';

/** Home hero — new lockup; optional Formless aside via `?heroBookAside=1` or env. */
export function HomeHero() {
  return <LayoutTestHeroSection />;
}
