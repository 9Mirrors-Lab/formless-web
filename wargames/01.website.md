WARGAME ORDER. You are not executing this mission, you are wargaming it. A cheaper executor (Sonnet) runs the brief below later. Your job is the route it will follow.
Recon first, read-only: the reference site and anything the build must match.
Then fight the mission on paper, move by move, and write it to wargames/01-website.md:


every move states its expected observation, exactly what you should see if it worked

every move carries its most likely failure, the cause it signals, and the counter-move

every fork gets a trigger, if you observe X, take route B

assumptions recon could not settle get marked RECON NEEDED with the exact check that settles it

end with abort conditions, and the verification runs the executor must perform with what pass looks like for each
Write it so the executor can run the brief end to end without asking a single question.
=== THE MISSION BRIEF (the executor's orders, not yours) ===
I'm rebuilding the Eyes Closed Client Section (working name: Client Room / Design Lab) for Eyes Closed (eyesclosed.love / formless-web) because the current one hides navigation in a tiny bottom-right flask, dumps clients into raw pages and bare HTML specimens, and gives us no control over how a direction is framed. It feels like an internal moodboard, not a branded client room. Visitors are non-technical clients reviewing design directions with the Eyes Closed team (shared call or solo after a link), and the one action I want from them is to enter a curated direction, sit with a controlled presentation of the work, compare options without getting lost, and return to the Client Section index ready to say which direction feels true.
This is not a public marketing rebuild. It is a branded Client Section with full control over representation: we present the design, we do not blast the page. Directions are shown inside a deliberate frame (stage, plate, or viewport), with title, intent, status, and room chrome always owned by us. Opening a direction never strands the client in an unframed full-bleed artifact with no way home.
The live Eyes Closed site will keep evolving the way Science already does: sections deepen over time with stronger graphic language when the teaching needs it. The Client Section must make room for that evolution. Graphics are a first-class tool when they earn their place (icon systems, field/torus marks, shader atmosphere, science diagrams, direction-specific art). They are not a quota. Do not add illustration, icon rows, or decorative marks because a layout "needs something visual." If type, space, and the framed work already carry the idea, leave it. When a direction is graphic-led (Science, Icons, Moodboard, Backgrounds), let the graphic be the hero inside the stage. When a direction is typographic or structural (About layouts, section templates), keep the stage quiet and let composition speak.
Build a complete static prototype of the Client Section in ./site. Plain HTML, CSS, and JS. No frameworks, no build step, opening index.html in a browser shows the finished site. Sections:


Branded Client Section identity on every surface: quiet Eyes Closed mark + "Client" (or "Client Room") label. This room should feel intentional and owned, not like a hidden /design-lab utility.

Persistent client chrome on every page (top rail or left rail, always visible, never a corner icon): Client home, current direction label ("You are here"), other directions, exit back to the public site. Primary wayfinding is large enough to scan in a meeting.

Client index as the review hub: large direction targets grouped In progress / Directions / Foundations / Atmosphere. One short intent line each. Entry plates preview the work as a framed thumbnail only when a real visual exists; otherwise a calm typographic plate is correct. No fake graphics to fill empty thumbs.

Direction stages (not raw page dumps): each catalog entry opens a stage page that keeps Client chrome, shows direction name + intent, and presents the work inside a controlled frame (e.g. device/viewport plate, cropped hero plate, or sequenced boards). Include mock stages for at least: Hero & section templates, Science dark directions, About layout studies, Supporting page themes, Design foundations, Iconography, Moodboard, Backgrounds.

Multi-board directions: when a direction has variants (e.g. Science: Vault / Atelier / Observatory), present them as sequenced boards or selectable plates inside the same stage, still inside Client chrome. Never send the client to an orphan HTML file with its own competing nav.

Graphic discipline by direction: Science / Icons / Moodboard / Backgrounds may lead with marks, diagrams, or atmosphere. About / layout / theme stages stay composition-first. Prove both modes in the prototype so the room clearly supports an evolving, graphic-capable site without forcing art into every board.

Short client intro on the index only: what this room is, how to use it (enter → sit with the framed work → return → tell us which feels true). No comment forms required in v1.

Motion, high-end and tight (presence and hierarchy, not noise). Required:


Page enter: one short staged reveal (chrome first, then title, then stage content). Custom easing only (no default ease-in-out). Prefer transform + opacity only.

Index: light stagger on direction plates; hover is restrained (lift/opacity/hairline, no bounce, no glow spam).

Stage transitions: when switching boards or returning home, crossfade or short slide with a shared frame so the room feels continuous.

Cap: 2–3 intentional motion moments per view. No scroll-jacking, no perpetual ambient loops, no decorative particle systems.

Respect prefers-reduced-motion: instant states, no stagger.

Mobile first. Primary nav usable at 375px without a hidden floating button. No horizontal scroll at 375px.
Match this reference for tone and palette: Eyes Closed brand language (dark cream-on-ink field, quiet serif display, mono labels, restrained clay/sage accents, subtle noise if already in system). Editorial, precise, calm authority. Client-tool clarity and curated framing beat boutique quietness when they conflict. Treat the Science page direction work as the model for "graphics when the idea needs them," not as a mandate to ornament every stage. Do not copy the flask picker. Do not invent purple SaaS glass, cream-and-terracotta brochure defaults, or dashboard card grids. Do not use Inter/Roboto/Arial. Do not sprinkle icons or diagrams to decorate empty space.
Source of truth for the live catalog (recon only; do not require the executor to edit the React app in this mission): /Users/m4/formless-web, especially src/data/designLabCatalog.ts, src/pages/DesignLabPage.tsx, src/components/design-lab/DesignLabPicker.tsx, and public/design/*.html specimens. Specimens inform content only; the prototype must re-present them inside Client stages, not iframe the whole raw page as the product.
Semantic landmarks, labeled form inputs if any forms exist, alt text on every image.
When you believe you are done, verify before reporting. Open each page, exercise every link and interactive board switcher, confirm chrome never disappears, confirm no direction opens as an unframed orphan page, confirm reduced-motion still works, and confirm at least one graphic-led stage and one composition-led stage exist with no decorative filler art on the quiet stages. Audit each claim in your final summary against something you actually ran or read in this session.
Do the simplest thing that works well. No features, no abstractions, nothing beyond this list.